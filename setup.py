from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Detects and classifies ransomware using various models',
    author='Tristan Paul',
    license='',
)
